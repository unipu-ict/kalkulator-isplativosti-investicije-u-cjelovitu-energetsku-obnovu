<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.karlo.kalkulator_isplativosti_investicije_u_energetsku_obnovu">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" >
        <activity android:name=".splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ZidActivity" />
        <activity android:name=".StolarijaActivity" />
        <activity android:name=".KrovActivity" />
        <activity android:name=".KotaoActivity" />
        <activity android:name=".RasvjetaActivity" />
        <activity android:name=".RezultatiActivity"></activity>
    </application>

</manifest>